# Сити Маркет — построчная сверка с ТЗ v0.6

Источник ТЗ: `/Users/daniil/Downloads/Техническое задание v6.txt`
Дата сверки: 2026-06-03
Метод: построчное чтение ТЗ v6 + глубокая проверка по реальному коду (`src/db/schema.ts`, `src/lib/**`, `src/app/**`), а не по устаревшему v5-анализу.

> Этот файл заменяет `TZ_GAP_ANALYSIS.md` (он сделан по ТЗ v5 и местами устарел). Старый файл оставлен как история.

> **Ревизия от 2026-06-03:** проведён полный второй проход с чтением исходников (а не чекпоинтов).
> Code-verified напрямую: схема БД; вся бизнес-логика (`orders`, `invoices`, `documents`, `auth`, `catalog`, `cart`, `product-import`, `company-validation`); публичные страницы (главная, каталог, карточка товара, корзина, checkout, регистрация, логин); ЛК (заказы список+карточка, документы, уведомления, профиль, компания); кабинет продавца (`/seller`, заказ продавца); админка (дашборд, заказы список+карточка, документы, компании, продавцы карточка, категории). Исправлены переоценённые ✅ в §8.2, §11.2, §11.4, §17.6, §17.7, §18.3, §18.6, §21.3, §21.6, §22.3 и зафиксированы баги (§21.5 и др., см. «Найденные баги»).
> **Догон от 2026-06-03 (формы + хелперы):** дочитаны защитные роуты (`documents/[versionId]/download`, `files/[fileId]`), `auth/session`+`password` (scrypt, httpOnly/secure cookie, токен хешируется — §33 ✅), `notifications/helpers`, `content/queries` (баннеры по датам), `sellers/report`, `orders/queries` (доступ по `buyerCompanyId`, история статусов), admin-actions (`user`/`order`/`company-join` — блокировки, аудит; статус «Закрыт» добавлен в P0-02), `admin/users/[id]`, `account/company` (§17.4 все поля ✅), `system-events` (§21.2/§27.6 ✅), `checkout/success` (§26.5 ✅), `content-page-form` (редактор = textarea, §24 🟡). Аудит критических действий покрыт широко → §49 логирование фактически ✅ (отдельного UI журнала нет, только system-events + действия в карточке пользователя).
> Прочитанным охватом считаю всё ТЗ-значимое. Не открывал только тривиально-подтверждающие: формы создания товара/категории/баннера и мелкие компоненты (`product-card`, `add-to-cart`, `cart-line-controls`) — их поведение выведено из схемы, списков и actions. Адаптив (§25) и полный набор состояний экранов (§26) по коду не верифицируются — нужна браузерная проверка.

## Легенда статусов

- ✅ **Готово** — есть рабочий сценарий, подтверждён по коду.
- 🟡 **Частично** — есть база/часть, но по ТЗ ещё не закрывается.
- ❌ **Не сделано** — требуется в v1, отсутствует.
- ⛔ **Блок (заказчик)** — нужен ответ/данные/доступ заказчика до реализации.
- ⬜ **Вне v1** — ТЗ явно исключает из первой версии (контроль, что не сделали лишнего).
- ℹ️ **Декларация** — пункт без отдельной реализации (роль/ограничение/принцип).

---

## 🔴 Критические системные расхождения (исправить в первую очередь)

Эти вещи затрагивают много пунктов сразу:

1. ✅ **НДС 22% исправлен кодом P0-01.** Дефолт `products.vatRate`, форма товара, Excel-импорт, seed и fallback расчёта заказа переведены на `22.00`; добавлена миграция `0003_vat_rate_22.sql` для default и существующих товаров со ставкой `20.00`/`NULL`. Затрагивает: §3.4, §9.4, §10.1/10.2, §11.2, §14.4, §22.2, §28.6, §28.8.
2. ✅ **Бизнес-статус «Закрыт» добавлен кодом P0-02.** `orderStatusEnum` = `new, awaiting_payment, paid, issued, closed, cancelled`; админские фильтры/экспорт/карточка заказа принимают `closed`, label = «Закрыт», дашборд показывает «Выданные» и «Закрытые», в карточке заказа добавлена подсказка про УПД после закрытия. Затрагивает §13.1, §13.3, §36.6, §15.7.
3. ✅ **Лимит документов 50 МБ исправлен кодом P0-03.** `maxDocumentSizeBytes = 50 * 1024 * 1024` (`documents/actions.ts`), текст ошибки обновлён на «не больше 50 МБ». Чат-вложения останутся в P1, потому что чата ещё нет.
4. ✅ **Исключённые типы документов убраны из выбора кодом P0-04.** В selectable-массивах `documents/types.ts` оставлены только v1-типы; labels для `invoice_factura`, `waybill`, `certificate`, `passport`, `warranty`, `manual` сохранены только для отображения старых документов.
5. **Чат + Telegram не реализованы.** Есть только таблицы `chats`/`messages` и счётчики ошибок Telegram в дашборде. Нет UI чата в ЛК, отправки, бота, webhook. Затрагивает §17.8, §19 целиком, §31.1.
6. **Нет реальной отправки email (только очередь `email_outbox`).** Затрагивает всю матрицу email-уведомлений §20 и §31.
7. **Нет подгрузки данных по ИНН, восстановления и смены пароля.** §16.2, §16.7, §17.3.
8. ✅ **Нумерация ORD/INV/CM переведена на PostgreSQL sequences кодом P0-07.** `numbering/sequences.ts` использует `nextval()` + проверку unique index; миграция `0005_number_sequences.sql` создаёт sequences и стартует их от текущих max ORD/INV/CM.

---

## 🐛 Найденные баги (не только «не сделано», а именно дефекты)

1. ✅ **Список компаний `/admin/companies` — сдвиг колонок исправлен кодом P0-08.** Порядок ячеек теперь соответствует заголовкам: `Документы | Статус | Создана | Обновлена`.
2. ✅ **Карточка заказа админа `/admin/orders/[id]` — продавец по позиции исправлен кодом P0-09.** Запрос `orderItems` делает join на `sellers`, в составе заказа показан продавец по каждой позиции, добавлена сводка комиссии по продавцам.
3. ✅ **Корзина/checkout — итог НДС исправлен кодом P0-10.** `CartView` теперь считает `vatAmount` по позиции и `vatTotal`; корзина и checkout выводят НДС по позиции и «Итоговую сумму НДС».

---

## 1. Статус документа
ℹ️ Декларативный раздел. Деление «Зафиксировано / Ожидаем от заказчика» учтено ниже статусами ⛔.

## 2. Общая информация о проекте

- 2.3 Адаптив клиентской части (Desktop/Tablet/Mobile) — 🟡 делалось точечно, полная приёмка по §25 не проведена.
- 2.3 Админка только desktop, без мобильной адаптации — ✅ соответствует.
- 2.4 Аудитории (покупатель, закупщик, продавец, админ, оператор) — 🟡 роли buyer/seller/admin есть (`userRoleEnum`); «оператор» — отдельной сущности нет, он работает через Telegram (которого нет). «Закупщик» = пользователь компании, отдельной под-роли нет (и не требуется по §4.2 п20).
- 2.5 Назначение (список из 10 возможностей покупателя): просмотр каталога ✅, поиск ✅, корзина ✅, оформление ✅, автосчёт ✅, документы в ЛК ✅, загрузка документов компании ✅, **чат с оператором ❌**, история заказов ✅, повтор заказа ✅.
- 2.5 Возможности администратора (19 пунктов) — см. детально §21; большинство ✅, чат ❌, часть документов 🟡.

## 3. Ключевые продуктовые решения

### 3.1 Маркетплейс-модель (11 пунктов)
- Единое окно для покупателя ✅; оплату получает маркетплейс ℹ️; один счёт на заказ ✅; заказ с товарами разных продавцов ✅; позиция хранит продавца ✅ (`order_items.sellerId`); продавец для внутр. учёта/комиссии/кабинета ✅; продавец не выставляет счёт ✅ (нет такого функционала); продавец не общается с покупателем ✅ (чата нет вовсе); товары размещает админ ✅; продавец не управляет витриной ✅.

### 3.2 Счёт и юридическая модель (12 пунктов)
- Счёт автоматически после заказа ✅; PDF ✅; в ЛК ✅; **на email — 🟡 только очередь**; виден админу ✅; от юрлица маркетплейса 🟡 (реквизиты-плейсхолдер); один заказ = один счёт ✅ (`uniqueIndex invoices_order_idx`); состав после счёта не редактируется ✅ (нет редактирования состава); изменение через отмену+новый заказ 🟡 (отмена только админом, покупателю — «повторить заказ»).

### 3.3 Документы v1 (14 пунктов)
- Автоматически только счёт ✅; остальные вручную ✅; ручные типы (договор/УПД/спецификация/акт) — присутствуют как типы ✅; счёт-фактура/накладные/сертификаты/паспорта/гарантийные/инструкции убраны из выбора ✅ (labels оставлены для архивных записей); покупатель грузит карточку компании + уставные ✅; продавец грузит УПД ✅; хранение ✅; у документа есть тип/владелец/связь/версия/статус/видимость ✅ (см. §15.3); при замене старая версия не удаляется ✅; покупатель видит только актуальную ✅; продавец видит только актуальную свою ✅; админ видит историю версий ✅; админ может скрыть через статус ✅; закрытые ссылки с проверкой прав ✅.

### 3.4 НДС (6 пунктов)
- ✅ **Ставка 22% реализована кодом P0-01.** Отображение/расчёт НДС в заказе ✅ (механика есть), в счёте ✅ (механика есть), в Excel-импорте ✅ (механика есть), дефолт при импорте — `22%`.

### 3.5 Комиссия (8 пунктов)
- Комиссия 5% по умолчанию ✅ (`sellers.commissionRate default 5.00`); считается по позиции ✅ (`order_items.commissionAmount`); не показывается покупателю ✅; видна админу ✅; видна продавцу ✅; управленческий расчёт ✅; автовыплат нет ✅ (соответствует); бух-документы по комиссии не формируются ✅; история выплат — 🟡 в отчёте продавца есть ручной статус выплаты, отдельной истории периодов нет.
- Формула: сейчас комиссия = `lineTotal * rate` от **всех** созданных позиций, а ТЗ §3.5 — «от суммы **оплаченных** позиций». В кабинете продавца финансы считаются по оплаченным заказам, но `commissionAmount` фиксируется на всех. 🟡
- ⛔ Ожидаем заказчика: комиссия с НДС или без (сейчас фактически с НДС); нужна ли история выплат.

## 4. Состав первой версии

### 4.1 Входит (51 пункт) — сводно
✅ Готово: 1 Главная, 2 Каталог, 3 Категории, 4 Подкатегории, 5 Карточка товара, 6 Поиск, 7 Сортировка, 8 Фильтры, 9 Корзина, 10 Оформление, 11 Регистрация ЮЛ/ИП, 12 Авторизация, 15 ЛК покупателя, 18 История заказов, 19 Документы по заказам, 20 PDF-счёт, 25 Кабинет продавца, 26 Комиссия 5%, 27 Админка desktop, 28 Управление товарами, 29 Ручное добавление, 30 Импорт Excel, 31 Категории/подкат, 32 Заказы, 33 Статусы заказов, 34 Пользователи, 35 Компании, 36 Продавцы, 37 Документы, 38–41 загрузка договора/УПД/спец/акта админом, 42 УПД продавца, 43 Версионность, 44 Видимость, 45 Скрытие, 46 Баннеры, 47 Инфостраницы, 48 Экспорт заказов.

🟡 Частично: 49 Фиксация критических действий (audit есть, не весь UI), 50 Адаптив (точечно). Проверка обязательных документов перед заказом исправлена в P0-05; загрузка документов покупателем по лимиту 50 МБ исправлена в P0-03.

❌ Не сделано: 13 Восстановление пароля, 14 Подгрузка по ИНН, 21 Отправка PDF на email (только очередь), 22 Чат с оператором, 23 Telegram-интеграция, 24 Резервный просмотр чатов в админке, 51 Техническая инструкция админа — 🟡 есть черновик `ADMIN_INSTRUCTION.md`.

> ✅ P0-05: заказ блокируется без активных документов компании `company_card` и `charter`. Проверка вынесена в `company-documents.ts`, применяется на `/checkout` и в backend `createOrderAction`.

### 4.2 Не входит (30 пунктов)
⬜ Контроль «не сделали лишнего»: 1С ⬜, ЭДО ⬜, онлайн-оплата ⬜, автоподтверждение оплаты ⬜, автовыплаты ⬜, автобух-доки ⬜, автогенерация договора/спец/УПД/счёт-фактуры/акта ⬜, счёт-фактура/накладные/товарные накладные/гарантийные/сертификаты/паспорта/инструкции убраны из выбора документов ✅ (labels сохранены для архивных записей), сложные роли в компании ⬜, индивид. цены ⬜, BI/аналитика ⬜, мобильное приложение ⬜, автоперсонализация ⬜, склад ⬜, расчёт доставки ⬜, мобильная админка ⬜, самопубликация товара продавцом ⬜, прямой чат продавец-покупатель ⬜.

## 5. Пользовательские роли

### 5.1 Гость (может 11 / не может 10)
- Может: главная ✅, каталог ✅, категории ✅, подкатегории ✅, карточка ✅, цены ✅, поиск ✅, добавить в корзину ✅, инфостраницы ✅, перейти к регистрации/входу ✅.
- Не может: оформить заказ ✅ (редирект на вход), счёт ✅, ЛК ✅ (guard), документы ✅, чат ✅ (нет вовсе), закрытые документы ✅, история заказов ✅, документы компаний ✅, кабинет продавца ✅, админка ✅.

### 5.2 Покупатель (может 24 / не может 11)
- Может: авторизация ✅, каталог/поиск/корзина ✅, оформление при условиях ✅ (реквизиты + обязательные документы проверяются после P0-05), автосчёт ✅, **счёт на email 🟡**, история ✅, карточка заказа ✅, документы заказа ✅, договор компании ✅, УПД/спец/акт по заказу ✅, загрузка карточки/уставных ✅, замена документов ✅, редактирование перс. данных ✅ (кроме пароля ❌), редактирование данных компании ✅, **чат + вложения ❌**, повтор заказа ✅, уведомления в ЛК ✅, **email о новых документах 🟡 (очередь/ин-апп, реальный email нет)**.
- Не может (ограничения): без обязательных документов оформить ✅ (backend + checkout блокировка после P0-05); менять статус ✅ нельзя; грузить от имени админа ✅; видеть чужие компании ✅ (доступ по `buyerCompanyId`); видеть документы продавца ✅; видеть архивные версии ✅; управлять товарами/категориями/продавцами ✅; видеть комиссию ✅; видеть админку ✅.

### 5.3 Продавец (может 13 / не может 10 + 5 зафиксировано)
- Может: вход в кабинет ✅, профиль/реквизиты ✅, загрузка УПД ✅, замена УПД с версиями ✅, свои документы ✅, свои товары ✅, заказы со своими товарами ✅, суммы продаж ✅, комиссия ✅, сумма к выплате справочно ✅, скачать отчёт ✅ (`/seller/orders/export`), **уведомления в кабинете — 🟡 (отдельного раздела нет)**.
- Не может: публиковать товары ✅, менять цены ✅, скрывать товары ✅, менять статусы ✅, выставлять счета ✅, грузить документы за админа ✅, видеть чужие документы ✅, видеть чужие заказы ✅ (фильтр по `seller_id`), прямой чат ✅ (нет чата), грузить лишние документы — 🟡 выбор типов продавца ограничен v1-типами после P0-04, но server action всё ещё принимает строковый тип.
- Зафиксировано: документы продавца видят продавец+админ ✅; покупатель не видит ✅; уведомление админу при загрузке УПД ✅ (`insertAdminNotifications`); замена УПД ✅; старые версии сохраняются ✅.

### 5.4 Администратор (37 пунктов «может»)
В основном ✅ (детали в §21). Отмечу несоответствия: п.16 «Загрузить УПД по заказу» ✅, п.24 «Повторно сформировать счёт» ✅, п.27 «Просматривать чат» ❌, п.28 «Отвечать в чат» ❌, п.29 «Ошибки email» 🟡 (счётчики есть, реального email нет), п.30 «Ошибки Telegram» 🟡 (счётчик есть, Telegram нет), п.31 «Ошибки генерации счёта» ✅.

### 5.5 Оператор/менеджер (7 пунктов)
- ❌ Полностью завязан на Telegram, которого нет. «Система сохраняет все сообщения в БД независимо от Telegram» — таблицы есть, поток нет.

## 6. Матрица прав доступа
- ℹ️ Требование «права на backend, не только скрытие кнопок» — ✅ соблюдается: guards `requireUser([...])` во всех server actions и layout'ах (`/account`,`/seller`,`/admin`), доступ к заказам/документам по `buyerCompanyId`/`sellerId`, защищённый download (`/documents/[versionId]/download`).
- Строки матрицы по ролям в основном ✅. Строки про **чат** (покупатель: Да / админ: Просмотр-резервно) — ❌. Строка «Просмотр комиссии: продавец только свою / админ» — ✅. «Удаление документа физически — Нет для всех» — ✅ (есть только `hideDocument`, физического удаления нет).

## 7. Структура сайта

### 7.1 Клиентская часть (17 пунктов)
✅ Главная, Каталог, Страница категории (`/catalog/[categorySlug]`), Подкатегории (`/catalog/[categorySlug]/[subcategorySlug]`), Карточка товара, Корзина, Оформление, Успех оформления, Регистрация, Вход, ЛК покупателя, Кабинет продавца, Инфостраницы, Контакты (как инфостраница).
🟡 Страница поиска — отдельного route нет, поиск через `/catalog?q=`.
❌ Восстановление пароля (route нет), Чат.

### 7.2 ЛК покупателя (9 разделов)
✅ Персональные данные (`/account/profile`), Данные компании (`/account/company`), Документы компании (`/account/documents`), Заказы (`/account/orders`), Карточка заказа (`/account/orders/[id]`), Документы по заказам (внутри карточки), Уведомления (`/account/notifications`).
🟡 Дашборд/обзор — `/account` это сетка-навигация («каркас»), не дашборд по §17.2.
❌ Чат.

### 7.3 Кабинет продавца (9 разделов)
✅ Дашборд (`/seller`), Профиль, Реквизиты, Документы, Товары, Заказы (+ карточка `/seller/orders/[id]`), Финансы/комиссия, Отчёты (export).
🟡 Уведомления — отдельного раздела нет.

### 7.4 Админка (24 раздела)
✅ Авторизация, Дашборд, Заказы, Карточка заказа, Товары, Создание/редактирование товара, Категории, Подкатегории, Пользователи, Компании, Заявки на присоединение, Продавцы, Документы, Баннеры, Инфостраницы, Импорт, Экспорт, Системные события.
🟡 Договоры/УПД/Спецификации/Акты — отдельных разделов меню нет, всё через общий «Документы» + карточку заказа/компании.
❌ Чат/сообщения, Настройки.

## 8. Главная страница

### 8.2 Структура (7 блоков)
✅ Шапка, Баннерный блок (слайдер 3-4), Блок категорий, Блок популярных/рекомендуемых товаров, Footer.
🟡 Блок «преимущества платформы» — есть 2 мини-карточки рядом с баннером («Для ООО и ИП», «Поставка с менеджером», `page.tsx:33-48`), полноценного блока преимуществ нет.
🟡 Блок «для продавцов/партнёров» — «если предусмотрено дизайном», сейчас нет.

### 8.3 Шапка (9 пунктов)
✅ Логотип/текстовый знак, Каталог, Поиск, Войти (гость)/Профиль, Кабинет продавца (для продавца — через профиль), Иконка корзины, Счётчик корзины, бейдж уведомлений, строка категорий. Реализовано в `public-header.tsx`.

### 8.4 Баннеры (11 полей + 6 типов ссылок)
✅ Все поля есть (`banners`): Название(`title`), Desktop-изобр., Mobile-изобр., Заголовок(`headline`), Подзаголовок(`subheadline`), Текст кнопки(`ctaText`), Ссылка(`href`), Порядок(`sortOrder`), Активность(`isActive`), Дата начала(`startsAt`), Дата окончания(`endsAt`).
✅ Ссылка может вести на категорию/подкат/товар/инфостраницу/внешний URL/никуда (есть подсказки ссылок `banner-link-suggestions`).

### 8.5 Категории на главной (8 категорий)
✅ Список категорий из ТЗ заложен в seed (`constants.ts marketplaceCategories`), карточки с названием и изображением.
⛔ Ожидаем заказчика: финальная структура подкатегорий, изображения категорий, порядок.

### 8.6 Footer (3 группы ссылок)
✅ Покупателям (FAQ, юр. инфо), Продавцам (как стать партнёром, условия), Компания (о нас, контакты, вакансии) — `site-footer.tsx`, ведут на `/info/[slug]`.

## 9. Каталог

### 9.2/9.3 Поля категории/подкатегории
✅ Все поля присутствуют (`categories`/`subcategories`): ID, Название, Slug, Изображение, Описание, Порядок, Активность, Даты создания/обновления; у подкат — родительская категория.

### 9.4 Список товаров (8 пунктов)
✅ Фото, Название, Артикул, Цена с НДС, Единица, Кнопка «В корзину», Ссылка на карточку. Неактивные скрыты (`isActive` фильтр).
✅ «Ставка НДС 22%» — дефолт и fallback переведены на 22%.

### 9.5 Сортировка (4 + 1)
✅ Цена ↑/↓, Новизна, Популярность. Сортировки по наличию нет — ✅ соответствует ТЗ.

### 9.6 Популярность
🟡 Реализован ручной флаг `isPopular` + дата. ТЗ требует считать по количеству добавлений в заказы (с fallback на ручной флаг) — авторасчёта по заказам нет.

### 9.7 Фильтры (4 + продавец)
✅ Категория, Подкатегория, Цена от/до, Единица измерения.
🟡 Фильтр по продавцу: в админ-списке товаров продавец — только колонка, отдельного фильтра нет. На клиенте продавец не показывается.
⛔ Ожидаем заказчика: видит ли покупатель продавца товара.

### 9.8 Поиск (5 областей + 6 правил)
✅ Поиск по названию/артикулу/категории/подкат/описанию (`ilike`). Мин. длина 2 (backend), регистронезависимо (`ilike`), частичные совпадения (`%...%`), пустой запрос → каталог, неактивные скрыты, пустое состояние есть.
🟡 Явной UX-обработки запроса < 2 символов на фронте нет (просто игнорируется).

## 10. Карточка товара

### 10.1 Состав (12 пунктов)
✅ Основное фото, доп. фото (галерея), Название, Артикул, Цена с НДС, Описание, Размер, Единица, Кнопка «В корзину», Блок «Рекомендуем также».
🟡 «Характеристики» — отдельного поля нет, только размер/единица (ТЗ это допускает).
✅ «Ставка НДС 22%» — дефолт и fallback переведены на 22%.

### 10.2 Цена и НДС (хранимые суммы)
✅ Цена с НДС, ставка НДС (поле есть), сумма НДС строки (`order_items.vatAmount`), сумма строки (`lineTotal`), итог заказа (`orders.totalAmount`), итог НДС (`orders.vatAmount`).
✅ Дефолтная ставка = 22%.

### 10.3 Артикул (формат CM-000001, 5 правил)
✅ Автогенерация `CM-` при ручном создании и импорте, уникальность (`products_sku_idx`), ручной ввод при импорте, обновление по артикулу при импорте.
✅ Автогенерация номера переведена на PostgreSQL sequence после P0-07.

### 10.4 Остатки / доступность
✅ Остатки не показываются, статус Активен/Неактивен, неактивный скрыт в каталоге, не добавляется в корзину (кнопка disabled), по прямой ссылке «Товар недоступен» (`product/[slug]/page.tsx:134`).

### 10.5 Рекомендации (5 правил)
✅ Подкатегория → категория → (популярные fallback частично), неактивные не показываются, без персонализации. 🟡 третий уровень (просто «популярные» по всем) реализован как добор по категории/популярности — близко к ТЗ.

## 11. Корзина

### 11.2 Состав (15 пунктов)
✅ Список, Название, Артикул, Цена с НДС, Количество, Единица, Сумма по позиции, Итог («Сумма с НДС»), «Оформить заказ», удаление, изменение количества, «← Назад» (вместо «Продолжить покупки»).
✅ «Фото товара» — `getCurrentCart` выбирает `mainImage`, в корзине выводится реальное фото или placeholder при отсутствии после P0-11.
✅ «Итоговая сумма НДС» (п.11) выводится в блоке «Итого» после P0-10.
✅ «НДС 22%» по позиции (п.6) выводится после P0-10; ставка для новых товаров/заказов 22% после P0-01.

### 11.3 Поведение (9 пунктов)
✅ Гость добавляет ✅, не оформляет ✅, редирект на вход ✅, авторизованный → оформление ✅, количество > 0 ✅, пересчёт ✅, неактивный товар исключается/предупреждение ✅, изменение цены — 🟡 (snapshot цены есть в `cart_items.priceSnapshot`, предупреждение реализовано по чекпоинтам, требует ручной проверки).
✅ «Перед оформлением проверка обязательных данных **и документов** компании» — реквизиты и активные документы `company_card`/`charter` проверяются на checkout и в `createOrderAction`.

### 11.4 Пустая корзина
✅ «Корзина пуста» + кнопка «Перейти в каталог» после P0-11.

## 12. Оформление заказа

### 12.1 Условия (8 пунктов)
✅ Авторизация, привязка к компании, реквизиты компании, активные карточка компании + уставные документы, товары в корзине, активность товаров, количество > 0.
✅ **Загружена карточка компании / уставные документы** — проверяется в `company-documents.ts` и `orders/actions.ts`.

### 12.2 Обязательные данные компании (8 полей)
✅ Проверяются через `getCompanyMissingFields` (`company-validation.ts`): название, ИНН, КПП (для ООО), ОГРН/ОГРНИП, юр. адрес, банк. реквизиты, контактный email/телефон. (Проверить точный состав в файле валидации.)

### 12.3 Обязательные документы
✅ Логика «оформление заблокировано до загрузки карточки компании + уставных документов» реализована. Статус «Документы загружены / не загружены» вычисляется в ЛК компании и checkout без нового поля в `buyer_companies`.

### 12.4 Процесс оформления (29 шагов)
✅ Большинство: проверки, фиксация цен, создание заказа, номер, позиции, продавец по позиции, расчёт суммы/НДС/комиссии, формирование PDF, сохранение, привязка к заказу, постановка email в очередь, страница успеха, заказ в ЛК и админке, ин-апп уведомление админу.
🟡 Шаг 25 «отправляет счёт на email» — только очередь.
❌ Шаг 9-11 в части документов (см. 12.3).

### 12.5 Комментарий к заказу
✅ Поле есть (`orders.comment`), виден покупателю и админу. Оператору в чате — ❌ (чата нет).

### 12.6 Доставка
✅ На `/checkout` добавлена точная формулировка: «Доставка и условия получения заказа согласовываются с менеджером после оформления заказа».

### 12.7 Повтор заказа (8 пунктов)
✅ `repeatOrderAction`: открыть заказ, «Повторить», активные товары в корзину, неактивные пропускаются, уведомление о пропущенных, актуальные цены, старые цены не применяются.

## 13. Статусы заказа

### 13.1 Бизнес-статусы (6)
✅ Реализовано 6: Новый, Ожидание оплаты, Оплачен, Выдан, Закрыт, Отменён.

### 13.2 Технические состояния (12)
🟡 Частично через `orders.technicalState` (jsonb): invoiceGenerating/invoiceGenerated/invoiceError/emailSent. Состояния «Договор загружен/УПД загружен/Спецификация/Акт/Ошибка загрузки документа/Документ скрыт/Документ заменён» как технические флаги заказа не ведутся (есть через таблицу documents/audit, но не в `technicalState`).

### 13.3 Логика переходов
✅ Корзина→Новый→Ожидание оплаты (после счёта авто-перевод). Ожидание→Оплачен, Оплачен→Выдан, Выдан→Закрыт и отмена доступны администратору через статус заказа.

### 13.4 Кто меняет статусы
✅ Система ставит Новый/Ожидание оплаты; админ — Оплачен/Выдан/Закрыт/Отменён (`order-actions.ts allowedStatuses`).

### 13.5 Отмена заказа
✅ Покупатель сам не отменяет (кнопка «Редактировать заказ» = добавить в корзину, чекпоинт 31); админ отменяет вручную. «Через чат к оператору» — ❌ (чата нет).

### 13.6 Редактирование заказа
✅ Состав после счёта не редактируется; «изменить» = новый заказ (повтор в корзину). Авто-формирование нового счёта при новом заказе ✅.

## 14. Счёт на оплату

### 14.1 Общая логика
✅ Счёт формируется после создания заказа, фиксации состава, расчёта НДС/итога, присвоения номера.

### 14.2 Получено от заказчика
ℹ️ Шаблон/пример/подпись/печать/логотип/НДС22/прописью/обязательные поля — это входные данные. Реализация см. 14.4.

### 14.3 Ожидаем от заказчика
⛔ Реквизиты юрлица, изображение подписи, изображение печати, финальный логотип, формат номера счёта, бух-подтверждение шаблона.

### 14.4 Состав PDF-счёта (36 пунктов)
🟡 Реализовано в `invoices/pdf.ts` частично:
- ✅ Номер счёта, Дата, Номер заказа, Название покупателя, ИНН покупателя, Юр. адрес покупателя, Список товаров, Артикулы, Единицы, Количество, Цена за ед. с НДС, Сумма по позиции, Сумма НДS по позиции, Итог с НДС, Итог НДС.
- ❌/🟡 **Логотип (картинка)** — только текст «Сити Маркет»; **Реквизиты маркетплейса** — плейсхолдер «будут уточнены»; **ИНН/КПП/ОГРН/юр.адрес/банк/р.сч/к.сч/БИК маркетплейса** — нет; **КПП/ОГРН покупателя** — нет; **Ставка НДС 22% колонкой** — нет (показывается сумма НДС, не ставка); **Сумма прописью** — нет; **Подпись** — нет; **Печать** — нет.

### 14.5 Нумерация (INV-000001, 6 правил)
✅ Уникальность (`invoices_number_idx`), автоприсвоение через PostgreSQL sequence, фиксация в PDF, неизменность, счёт остаётся при отмене, новый счёт при новом заказе.

### 14.6 Ошибка формирования счёта (7 пунктов)
✅ Заказ сохраняется (создаётся до генерации PDF), статус invoice `failed`, `technicalState.invoiceError`, сообщение покупателю на success/в карточке, ошибка видна админу, кнопка повторной генерации, запись в `system_events`. Полностью соответствует.

## 15. Документооборот

### 15.1 Общая логика (10 пунктов) — ✅ соответствует (счёт авто, остальное вручную, продавец УПД, счёт-фактура исключена из выбора после P0-04).

### 15.2 Типы документов (8 типов)
✅ В выборе есть v1-типы: Счёт, Договор, УПД, Спецификация, Акт, Карточка компании, Уставные документы, Прочий. Для контекстов есть отдельные массивы `orderDocumentTypes`, `buyerCompanyDocumentTypes`, `sellerDocumentTypes`. Labels лишних/исторических типов сохранены только для отображения старых записей. Архитектура расширяемая (varchar type + target enum) — ✅.

### 15.3 Общие поля документа (20 пунктов)
✅ Большинство через `documents` + `document_versions` + `files`: тип, название, файл, оригинальное имя (`files.originalName`), системное имя (`storageKey`), формат (`mimeType`), размер (`sizeBytes`), автор (`uploadedById`), тип автора 🟡 (выводится по роли загрузившего, отдельного поля «тип автора» нет), даты, связанная сущность (target+ид), версия, признак актуальности (`currentVersion`), статус (`isActive`), видимость покупателю/продавцу, комментарий админа (`document_versions.comment`), история замен (`document_versions`).
🟡 «Статус» документа в виде enum (Активен/Скрыт/Архивная/Заменён/Ошибка) — реализован упрощённо через `isActive` + версии, отдельного enum статуса нет.

### 15.4 Статусы документа (5)
🟡 Активен/Скрыт — через `isActive`. Архивная версия/Заменён — неявно (старые `document_versions`). «Ошибка загрузки» — нет статуса. Покупатель видит активные+актуальные ✅; админ видит всё ✅.

### 15.5 Версионность (8 пунктов)
✅ Физически не удаляются, новая версия при замене, новая становится актуальной, старая = архивная (неявно), покупатель/продавец видят актуальную, админ видит все, история замен в карточке.

### 15.6 Договор (15 пунктов)
🟡 Договор грузится админом (target `buyer_company` или `contract`), привязан к компании, виден покупателю/админу, версионность, скрытие, видимость продавцу — всё через общий механизм. ✅ уведомление покупателю в ЛК при видимой загрузке. **🟡 email при загрузке договора — только очередь.** «Один раз на компанию» — не форсируется (можно несколько). Отдельного UX «Договор как сущность» нет (есть target `contract` в enum, но без выделенного интерфейса).

### 15.7 УПД по заказу (13 пунктов)
🟡 Грузится админом по заказу, виден покупателю/админу/связанному продавцу, версионность, скрытие, уведомление в ЛК ✅, **email 🟡**. После P0-02 статус «Закрыт» есть, в карточке заказа добавлена организационная подсказка загрузить/проверить УПД после закрытия; жёсткая блокировка загрузки по статусу не вводилась.

### 15.8 Спецификация (13 + форматы)
🟡 Аналогично УПД (опциональный документ заказа). Email 🟡. Форматы PDF/DOCX/XLS/XLSX — ✅ поддерживаются загрузкой.

### 15.9 Акт (14 пунктов)
🟡 Тип `act` есть, грузится по заказу, версионность, видимость, уведомление в ЛК. Email 🟡.

### 15.10 Счёт-фактура
✅ ТЗ требует исключить: `invoice_factura` убран из selectable `documentTypes`; label оставлен для архивных записей.

### 15.11 Иные документы
✅ Накладная/товарная/гарантийные/сертификаты/паспорта/инструкции убраны из selectable `documentTypes`; labels оставлены для архивных записей.

### 15.12 Документы покупателя (12 пунктов)
✅ Покупатель грузит карточку компании + уставные, может позже, видит/заменяет (версии), админ видит/скачивает старые версии, уведомление админу при загрузке. ✅ «до загрузки документов заказ оформить нельзя» — форсируется после P0-05.

### 15.13 Документы продавца (9 пунктов)
✅ Продавец грузит УПД, видит свою актуальную версию, замена с версиями, видят продавец+админ, покупатель не видит, уведомление админу. 🟡 «дополнительные документы продавец не загружает» — тип не ограничен.

### 15.14 Договор с продавцом
🟡 Может храниться как документ (target seller/contract). Автонумерация договора продавца — ❌ нет отдельного поля/механизма (`sellers` не содержит номер договора). Автогенерация файла — ⬜ вне v1 (соответствует).

### 15.15 Форматы файлов (6)
✅ PDF, DOCX, JPG, PNG, XLS, XLSX (+ DOC) — `allowedExtensions`/`allowedMimeTypes`.

### 15.16 Размер файлов
🟡 Документы — лимит **50 МБ** после P0-03. Чат-вложения ещё не реализованы. Изображения товаров/баннеров — лимит 10 МБ ✅.

## 16. Регистрация и авторизация

### 16.1 Регистрация покупателя (8 полей)
✅ Тип ООО/ИП, ИНН, название, юр.адрес, email, телефон, пароль. 🟡 Банковские реквизиты в форме регистрации не собираются (по ТЗ они нужны до заказа — это допустимо, грузятся позже).

### 16.2 Подгрузка по ИНН
❌ Не реализовано (форма: «Пока без внешнего сервиса ИНН, вручную»).
⛔ Ожидаем: сервис (DaData/ФНС/Контур) + API-ключ.

### 16.3 Ошибка подгрузки по ИНН
🟡 Ручное заполнение работает по умолчанию; сообщения об ошибке подгрузки нет (т.к. нет подгрузки).

### 16.4 Подтверждение email/телефона
✅ Не требуется — соответствует (не реализовано намеренно).

### 16.5 Модерация аккаунта
✅ Обычная регистрация без модерации; присоединение к компании → заявка админу.

### 16.6 Авторизация
✅ Вход по email+паролю, редирект в ЛК/по роли/на `next`. Блокированный/pending → `error=inactive`.

### 16.7 Восстановление пароля (7 шагов)
❌ Не реализовано (нет route, нет токенов, нет email).

### 16.8 Несколько пользователей компании (5 пунктов)
✅ Новый ИНН → компания; существующий ИНН → заявка; админ подтверждает/отклоняет; после подтверждения доступ к заказам/документам компании.

## 17. Личный кабинет покупателя

### 17.1 Разделы (9) — см. §7.2 (дашборд 🟡, чат ❌, остальное ✅).

### 17.2 Дашборд (7 пунктов)
❌/🟡 Текущая `/account` — сетка-навигация, а не дашборд. Нет: последние заказы+статусы на дашборде, напоминание о незагруженных документах, ссылки на загрузку карточки/уставных, последние сообщения чата. Бейдж уведомлений — ✅.

### 17.3 Персональные данные (4)
✅ Имя/ФИО, Email (проверка уникальности), Телефон. ❌ Пароль (смена не реализована).

### 17.4 Данные компании (9)
✅ Все поля редактируются на `/account/company` (тип, название, ИНН, КПП, ОГРН/ОГРНИП, юр.адрес, банк. реквизиты, контактные email/телефон).

### 17.5 Документы компании (8 + ограничение)
✅ Загрузка карточки/уставных, просмотр, скачивание актуальной, тип, дата, статус, замена. Покупатель не видит старые версии ✅.

### 17.6 Заказы — список (12 колонок)
✅ После P0-12 список (`account/orders/page.tsx` + `getCurrentBuyerOrders`) содержит номер, дату, статус, сумму, НДС, количество товаров, признаки счёта/УПД/спецификации/акта, бейдж «Не просмотрен», переход в карточку и кнопку «Повторить заказ» в строке списка.

### 17.7 Карточка заказа покупателя (18 пунктов)
✅ Номер, дата, статус, товары, количество, цена, НДС% по позиции, сумма по позиции, итог + «НДС в сумме», комментарий, счёт (если сформирован), УПД/спец/акт (документы заказа), история статусов, «Повторить заказ».
🟡 Фото товара в составе — заглушка-иконка (`account/orders/[id]/page.tsx:115`).
❌ «Договор компании, если доступен» (п.12) — в карточке заказа не выводится (показываются только документы с привязкой к заказу, договор привязан к компании).
❌ «Ссылку в чат» (п.18) — чата нет.

### 17.8 Чат (5 пунктов)
❌ Полностью отсутствует UI/логика.

## 18. Кабинет продавца

### 18.3 Дашборд продавца (8 пунктов)
✅ Кол-во товаров, кол-во оплаченных заказов, сумма продаж, сумма комиссии, сумма к выплате (блок «Финансы»), последние заказы.
🟡 «Статус УПД продавца» как отдельный индикатор на дашборде не выводится (документы есть отдельным блоком). Уведомления — отдельного раздела нет.

### 18.4 Профиль продавца (9 полей) — ✅ readonly (название, ИНН, КПП, ОГРН/ОГРНИП, юр.адрес, контактное лицо, email, телефон, статус).

### 18.5 Документы продавца — ✅ загрузка/замена УПД, актуальная версия, дата, статус; админ видит все версии/автора/историю.

### 18.6 Товары продавца (9 полей) — 🟡 readonly: фото, артикул, название, категория, подкатегория, цена, единица, статус. ❌ Колонка «НДС» в таблице товаров продавца отсутствует (`seller/page.tsx:258-264`). Без редактирования ✅.

### 18.7 Заказы продавца (8 полей) — ✅ номер, дата, статус, свои товары, количество, сумма по своим товарам, комиссия, сумма после комиссии (`/seller/orders/[id]`).
⛔ Ожидаем заказчика: какие данные покупателя видит продавец; нужен ли продавцу экспорт (экспорт уже реализован — подтвердить, оставлять ли).

## 19. Чат и Telegram
❌ Весь раздел не реализован: доступность чата покупателю (19.2), логика сохранения+отправки (19.3), Telegram-уведомление с данными (19.4), вложения (19.5), обработка ошибки Telegram (19.6). Есть только таблицы и счётчики ошибок в дашборде.

## 20. Уведомления

### 20.1 Каналы (5)
🟡 Email — только очередь (нет SMTP). ✅ Ин-апп в ЛК. ✅ Ин-апп в кабинете продавца (через notifications). ❌ Telegram. ✅ Системные уведомления в админке (`system-events`, дашборд).

### 20.2 Матрица уведомлений (~18 событий)
- ✅ Ин-апп: новый заказ (админу), счёт сформирован (покупателю), статус изменён (покупателю), договор/УПД/спец/акт загружен (покупателю), документ покупателя/продавца загружен (админу), ошибка счёта (system event).
- 🟡 Email-варианты всех событий — только в очереди, реально не уходят.
- ❌ Telegram: новое сообщение оператору, ответ оператора покупателю, ошибки Telegram.
- ❌ Регистрация клиента → email «Регистрация выполнена» (не ставится в очередь).

## 21. Административная панель

### 21.2 Дашборд (15 пунктов)
🟡 Есть: счётчики заказов по статусам (новые/ожидание/оплачены/выданные/закрытые/отменённые), последние заказы, последние компании, последние сообщения, ошибки счетов/email/Telegram/system. Нет явных блоков: «компании без обязательных документов», «новые документы покупателей/продавцов» отдельными блоками (есть через notifications). Требует доводки до полного списка §21.2.

### 21.3 Заказы — список (16 колонок) + фильтры (7) + карточка (20) + действия (14)
✅ Список: номер, дата создания, компания, ИНН, пользователь(email), сумма, НДС, статус, наличие счёта, кол-во документов, комментарий, дата обновления, действия.
🟡 Отдельные признаки «наличие договора/УПД/спец/акта» в списке — общим счётчиком документов, не по типам.
✅ Фильтры: статус, дата, компания, ИНН, наличие документов, сумма. 🟡 фильтр по продавцу — нет в списке заказов.
✅ Карточка: номер, дата, статус, данные покупателя/компании, состав, продавец по каждой позиции, сумма/НДС по позиции, итог, комиссия по позиции и сводка комиссии по продавцам, комментарий, счёт, документы, история статусов, технические состояния, действия.
✅ Действия: открыть, изменить статус, закрыть заказ, скачать счёт, повторно сформировать счёт, загрузить УПД/спец/акт, скачать/заменить/скрыть документ, версии, отменить заказ, экспорт заказа.

### 21.4 Товары — список (12) + действия (11)
✅ Список: фото, артикул, название, категория, подкат, продавец, цена, НДС, единица, статус, дата обновления, действия. ✅ Действия: создать, редактировать, деактивировать/активировать, фото, привязка к продавцу, цена, НДС, характеристики (размер), популярность, импорт Excel.

### 21.5 Компании покупателей — список (13) + действия (11)
🟡 Список и карточка реализованы (`/admin/companies`): название, ИНН, КПП, тип, кол-во пользователей, заказов, сумма заказов, документы, даты, действия. ✅ Действия: открыть, редактировать, пользователи, заказы, документы, скачать, загрузить/заменить/скрыть договор, версии, подтвердить пользователя (через заявки).
🐛 **Баг колонок** в списке (см. блок «Найденные баги» №1): значения «Документы / Статус / Создана» перепутаны местами.
🟡 «Готовность к оформлению заказа» как явный признак — частично (есть статус active/blocked, нет документного статуса).

### 21.6 Продавцы — список (10) + действия (12)
🟡 `/admin/sellers` колонки: Продавец (название+ИНН), Контакты, Комиссия, Товары, Заказы, Статус, Обновлён. ❌ В списке нет колонок «Сумма продаж», «Наличие УПД продавца», «Номер договора с маркетплейсом». ✅ Действия (в карточке): создать, редактировать, активировать/деактивировать, товары, заказы, УПД, скачать УПД, версии, комиссия, скачать отчёт. ❌ «Номер договора с маркетплейсом» — поля в `sellers` нет.

### 21.7 Документы (14 пунктов)
✅ Загрузка документов компании/заказа/продавца, выбор типа, выбор объекта привязки, видимость, скачивание, замена, версии, скрытие, автор, дата, размер, формат. Физическое удаление закрытых — запрещено ✅.

## 22. Excel-импорт товаров

### 22.1 Форматы — ✅ XLSX/XLS (+ CSV).
### 22.2 Общие правила (11) — ✅ заголовки, 1 ряд = товар, пропуск пустых, проверка обязательных, **двухшаговый предпросмотр** (validate → commit), ошибки до импорта, обновление по артикулу, генерация при отсутствии, категория не найдена → ошибка строки. ✅ НДС по умолчанию — 22%.
### 22.3 Колонки (12) — ✅ артикул, название, категория, подкатегория, цена с НДС, единица, размер, описание, продавец (обязателен), статус, НДС. ❌ «Фото (ссылка/имя файла)» — колонка `imageUrl` читается в payload, но `applyResolvedRow` её **не применяет** к товару (фото при импорте не подтягивается, `product-import-actions.ts:490-548`).
⛔ Ожидаем заказчика: финальный пример Excel, правила загрузки фото.

## 23. Файлы

### 23.1 Типы файлов (14) — ✅ фото товаров/категорий, баннеры, счета, документы (договор/УПД/спец/акт/карточка/уставные/продавца), Excel импорт/экспорт. ❌ вложения чата (чата нет).
### 23.2 Публичные файлы — ✅ фото товаров/категорий/баннеры (`access: public`, route `/files/[fileId]`).
### 23.3 Закрытые файлы — ✅ счета/документы (`access: private`, проверка прав в download route). Вложения чата ❌.
### 23.4 Форматы — ✅ PDF/DOCX/JPG/PNG/XLS/XLSX.
### 23.5 Размер — 🟡 документы 50 МБ ✅; изображения 10 МБ ✅; чат-вложения ещё не реализованы.
### 23.6 Безопасность файлов (6 + запрещённые)
✅ Проверка формата (allowlist расширений+MIME → exe/bat/cmd/js/php заблокированы по принципу allowlist), проверка размера, закрытые не отдаются публично, проверка прав при скачивании, раздельное хранение (public/private storage).

## 24. Информационные страницы (7 страниц + требования)
✅ FAQ, юр.инфо, как стать партнёром, условия для продавцов, о нас, контакты, вакансии — через `content_pages`, ЧПУ `/info/[slug]`, редактирование в админке, без форм/карты. 🟡 Редактор — обычная `<textarea>` (подтверждено `content-page-form.tsx:52`), не WYSIWYG/визуальный (ТЗ §35 упоминает «простой визуальный редактор»).
⛔ Ожидаем заказчика: тексты страниц, контактные данные, юр. тексты, условия для продавцов.

## 25. Мобильная адаптация (15 экранов)
🟡 Делалось точечно (общая шапка, баннеры mobile-image, каталог). Полная приёмка по всем 15 экранам не проведена. Восстановление пароля/чат — экранов нет вовсе. Админка не адаптируется ✅ (соответствует).

## 26. Состояния экранов

### 26.1 Главная — 🟡 основное ✅, нет баннеров → fallback hero ✅; «нет категорий / ошибки загрузки» — требует проверки.
### 26.2 Каталог — ✅ товары, пустые категория/подкат/поиск, пустой текст. Ошибки загрузки — 🟡.
### 26.3 Карточка товара — ✅ активен, не найден (`notFound`), неактивен («Товар недоступен»), добавлен в корзину. Ошибки — 🟡.
### 26.4 Корзина — ✅ есть товары, пустая, неактивный товар, не авторизован. «Цена изменилась / ошибка пересчёта / документы компании не загружены» — 🟡.
### 26.5 Оформление — 🟡 авторизация, компания не заполнена, заказ создаётся/создан, ошибка счёта, счёт сформирован. «Нет карточки/уставных документов» реализовано на checkout после P0-05. Email-состояния — 🟡 (очередь).
### 26.6 ЛК покупателя — 🟡 есть/нет заказов, есть/нет документов, есть/нет уведомлений. «Обязательные документы не загружены» добавлено в ЛК компании после P0-05.
### 26.7 Кабинет продавца — ✅ есть/нет товаров/заказов/продаж, УПД загружен/нет. Ошибки — 🟡.
### 26.8 Документы — ✅ загружен/заменён/скрыт/архивный, ошибки формата/размера/прав, не выбран тип/сущность. «Файл > 50 МБ» возвращает понятную ошибку после P0-03.
### 26.9 Чат — ❌ (нет чата).
### 26.10 Админка — 🟡 таблица загружена/пуста; остальные ошибочные состояния — точечно.

## 27. Валидация и обработка ошибок

### 27.1 Общие — 🟡 обязательные поля, подсветка, понятные сообщения, submit-disabled (`submit-button.tsx`). Сохранение введённых данных при ошибке — частично (редиректы с `?error=`).
### 27.2 Регистрация — ✅ ИНН/email/телефон/пароль/название обязательны, проверка существующего email, длина пароля. ❌ Валидация корректности формата ИНН; «банк/карточка/уставные обязательны до заказа» — карточка/уставные форсируются при оформлении после P0-05.
### 27.3 Авторизация — 🟡 «Неверный email или пароль» ✅; «Аккаунт заблокирован» отдельным текстом нет — blocked и pending_join объединены в «Аккаунт еще не активен или заблокирован». На `/login` нет ссылки «Забыли пароль?» (§16.7); в UI показаны демо-доступы admin/seller/buyer — убрать перед продом.
### 27.4 Корзина — ✅ количество > 0, активность, существование. «Цена актуальна» — 🟡.
### 27.5 Оформление — ✅ авторизация, корзина не пуста, данные компании, обязательные документы, активность, сумма.
### 27.6 Генерация счёта — ✅ ошибка фиксируется, заказ сохраняется, админ видит, клиент видит сообщение. Проверки «не хватает реквизитов/логотипа/подписи» — 🟡 (реквизиты-плейсхолдер, явных проверок нет).
### 27.7 Документы — ✅ формат, размер, файл не загружен, нет прав, не найден, не выбран тип/объект.
### 27.8 Excel-импорт — ✅ формат, обязательные колонки, цена, категория не найдена, продавец не найден, дубли артикулов, лимит строк (1000).

## 28. Структура данных
Сверка полей по `schema.ts`:
- 28.1 Пользователь — ✅ все поля (`users`): id, имя, email, телефон, passwordHash, роль, компания/продавец, статус, даты.
- 28.2 Компания покупателя — ✅ id, тип, название, ИНН, КПП, ОГРН, юр.адрес, банк (jsonb), статус, даты. ✅ «Признак готовности к оформлению» вычисляется по реквизитам и активным документам `company_card`/`charter`. Документы (карточка/уставные/договор) — через `documents`.
- 28.3 Продавец — ✅ id, название, ИНН, КПП, ОГРН, юр.адрес, банк, контактное лицо, email, телефон, комиссия, статус, даты. ❌ «Номер договора с маркетплейсом» поля нет.
- 28.4/28.5 Категория/Подкатегория — ✅ все поля.
- 28.6 Товар — ✅ id, артикул, название, категория, подкат, продавец, основное фото, доп. фото (`product_images`), описание, цена с НДС, ставка НДС (22% дефолт), размер, единица, популярность, активность, даты.
- 28.7 Заказ — ✅ id, номер, пользователь, компания, статус, сумма, сумма НДС, комментарий, технические состояния (jsonb). Счёт/УПД/спец/акт/договор — через `documents`/`invoices`, не отдельными полями (норм).
- 28.8 Позиция заказа — ✅ все поля: id, заказ, товар, продавец, название/артикул/цена/НДС на момент, ставка НДС, сумма НДС, количество, единица, сумма по позиции, комиссия по позиции.
- 28.9 Документ — ✅ id, тип, файл (через versions), компания/заказ/продавец/чат/договор-привязка, автор, версия, актуальность, статус, видимость покупателю/продавцу, даты, комментарий. 🟡 «тип автора» отдельным полем — нет.
- 28.10/28.11 Чат/Сообщение — ✅ таблицы и поля есть (`chats`/`messages`), но не используются (UI/логики нет).
- 28.12 Баннер — ✅ все поля.
- 28.13 Инфостраница — ✅ все поля.

## 29. Связи сущностей (21 пункт)
✅ Соответствуют схеме (FK): пользователь↔компания, компания↔много пользователей, заказ↔компания/пользователь, заказ↔позиции, позиция↔товар (snapshot), позиция↔продавец, товар↔категория/подкат/продавец, документ↔компания/заказ/продавец/договор, договор↔компания, УПД/спец/акт↔заказ, УПД продавца↔продавец, чат↔пользователь/компания, сообщение↔чат.

## 30. Нумерация
- 30.1 Заказы `ORD-000001` — ✅ формат/уникальность/неизменность; автонумерация через PostgreSQL sequence после P0-07.
- 30.2 Счета `INV-000001` — ✅ автонумерация через PostgreSQL sequence + unique index.
- 30.3 Товары `CM-000001` — ✅ авто через PostgreSQL sequence/уникальность/ручной при импорте/обновление по артикулу.
- 30.4 Договор с продавцом — ❌ нет номера/сущности.
- 30.5 Документы — ✅ внутренний UUID, публичная нумерация не обязательна.

## 31. Интеграции

### 31.1 Интеграции v1 (6)
- ⛔/❌ Подгрузка по ИНН — нет.
- 🟡 Email-отправка — только очередь, нет SMTP.
- ❌ Telegram — нет.
- ✅ Генерация PDF-счёта — есть (но шаблон неполный).
- ✅ Импорт Excel — есть.
- ✅ Экспорт Excel — есть.

### 31.2 Интеграция с 1С — ⬜ вне v1; архитектурная готовность — ℹ️ заложена (snapshot-поля, нормализованные сущности).
### 31.3 ЭДО — ⬜ вне v1.

## 32. Инфраструктура и передача
⛔/❌ Всё инфраструктурное не закрыто и/или ждёт заказчика:
- 32.1 Домен — ⛔.
- 32.2 Хостинг/сервер/SSL/бэкап/защита админки — ❌ (есть `docker-compose.yml`, `drizzle.config.ts` для локалки; R2/S3 опционально готово).
- 32.3 Email-отправитель/SMTP — ⛔/❌.
- 32.4 Telegram-бот/токен/webhook/операторы — ⛔/❌.
- 32.5 Передача доступов + инструкция админа — 🟡 (черновик `ADMIN_INSTRUCTION.md`).

## 33. Безопасность
- 33.1 Пароли — ✅ только хеш (`scrypt`, `auth/password.ts`).
- 33.2 Доступ к данным — ✅ покупатель видит свою компанию, продавец свои данные, админ всё, оператор — н/д (нет).
- 33.3 Закрытые документы — ✅ проверка прав при скачивании.
- 33.4 Админка — ✅ защищена авторизацией; сложный пароль — организационно/seed.
- 33.5 Файлы — ✅ блокировка исполняемых (allowlist).

## 34. Нефункциональные требования
- 34.1 Производительность 300+ товаров — 🟡 архитектура позволяет, нагрузочно не проверялось; есть индексы.
- 34.2 Масштабируемость — ℹ️ заложена.
- 34.3 Резервное копирование — ❌ не настроено (инфра).
- 34.4 SEO/ЧПУ — 🟡 ЧПУ категорий/подкат/товаров/инфостраниц ✅; title/meta для каталога/категории/подкат/товара/инфо ✅; H1 на всех страницах — требует проверки.
- 34.5 Аналитика (Метрика не подключать) — ✅ соответствует.

## 35. Требования к дизайну
- 35.1 Светлые тона, Ozon-like — ✅.
- 35.2 Базовая визуальная система (логотип-знак, палитра, типографика, кнопки, карточки, формы, таблицы, уведомления, статусы) — 🟡 базово есть, брендбука нет (и не требуется); логотип в счёте — только текст.
- 35.3 Прототипы не требуются — ✅.

## 36. Пользовательские сценарии
- 36.1 Гость смотрит каталог → предложение войти — ✅.
- 36.2 Регистрация покупателя — 🟡 (без подгрузки по ИНН; напоминание о документах на дашборде — ❌).
- 36.3 Присоединение к компании — ✅.
- 36.4 Загрузка документов компании — ✅ (но не разблокирует заказ, т.к. блокировки и нет).
- 36.5 Оформление заказа — 🟡 (без проверки документов; email в очереди).
- 36.6 Админ обрабатывает заказ — 🟡 (статус «Закрыт» добавлен; есть организационная подсказка про УПД после закрытия, но нет жёсткой автоматизации).
- 36.7 Админ загружает договор — ✅ (email 🟡).
- 36.8 Покупатель пишет в чат — ❌.
- 36.9 Админ импортирует товары — ✅ (предпросмотр+подтверждение).

## 37. Критерии приёмки
- 37.1 Главная — 🟡 (нет блока преимуществ; адаптив не закрыт).
- 37.2 Каталог — ✅ (пустые состояния, сортировка, поиск, скрытие неактивных).
- 37.3 Карточка товара — 🟡 («характеристики» = размер/единица).
- 37.4 Корзина — 🟡 (блокировка по документам не реализована).
- 37.5 Заказ — 🟡 (комиссия/НДС считаются; email в очереди).
- 37.6 Счёт — 🟡 (нет логотипа-картинки/реквизитов/прописью/подписи/печати; email в очереди).
- 37.7 Документы — 🟡 (лимит 50 МБ исправлен; остаётся чистка лишних типов).
- 37.8 Чат — ❌.
- 37.9 Кабинет продавца — ✅ (близко; уведомления-раздела нет).
- 37.10 Админ-панель — 🟡 (нет чатов/настроек; полный дашборд §21.2 ещё не закрыт).

## 38. Тестовые сценарии
ℹ️ Раздел для QA. Реализованных автотестов в репозитории не обнаружено (Playwright заявлен в spec, но e2e-тестов нет). Ручные проверки описаны в `CHECKPOINTS.md`.

## 39. Контент и материалы от заказчика
⛔ Ожидаем (20 пунктов): реквизиты юрлица, подпись, печать, логотип, формат №счёта, бух-подтверждение, структура категорий/подкат, список товаров, фото, описания, характеристики, список продавцов и реквизиты, тексты инфостраниц/главной, материалы баннеров, email-отправитель, данные Telegram-бота, доступы к домену, ответственный за согласование.
✅ Зафиксировано/учтено в коде: форматы файлов, версионность, скрытие через статус, список ручных документов.

## 40. Этапность работ
ℹ️ Организационный раздел. Фактический прогресс: этапы 2–6 (проектирование/дизайн/frontend/backend/админка) — в значительной степени выполнены; этап 7 (тестирование) и 8 (запуск) — открыты.

## 41. Инструкция администратора (31 пункт)
🟡 Есть черновик `ADMIN_INSTRUCTION.md`. Нужно довести до всех 31 пунктов (в т.ч. по ошибкам email/Telegram, повторной генерации счёта, версиям документов).

## 42. Итоговые продуктовые ограничения (26)
ℹ️ Контрольный список принципов. Несоответствия повторяют выше: №3 (автосчёт ✅), №6 (счёт-фактуру исключить из выбора ✅ после P0-04), №23 (документы 50 МБ ✅ после P0-03; чат-вложения ещё нет), №18 (заказ не редактируется ✅), №21 (админка desktop ✅).

## 43. Что ещё ожидаем от заказчика для финализации
⛔ Сведено в §39 + по разделам: модель продавца (видит ли продавца покупатель, экспорт продавца, какие данные покупателя видит продавец), комиссия (с НДС/без, история выплат, отчёты), каталог (подкатегории/товары/фото/описания/характеристики/продавцы), Excel (колонки/фото/пример), инфраструктура (домен/email/Telegram/доступы).

## 44. Финальные положения
ℹ️ Декларативный раздел. Комментарии заказчика по документообороту учтены в §14–15.

---

## Сводный план доработок (приоритеты)

**P0 — соответствие бизнес-правилам (быстро, критично):**
1. НДС 22% везде (schema default, форма товара, импорт, расчёт заказа, миграция существующих данных).
2. ✅ Добавить статус «Закрыт» (`closed`) + переход Выдан→Закрыт + «УПД после закрытия» — сделано кодом P0-02.
3. ✅ Лимит файлов документов 50 МБ — сделано кодом P0-03.
4. ✅ Убрать из выбора типы документов вне v1 (счёт-фактура, накладные, сертификаты, паспорта, гарантийные, инструкции) — сделано кодом P0-04.
5. ✅ Блокировка оформления заказа без загруженных карточки компании и уставных документов + вычисляемый документный статус компании — сделано кодом P0-05.
6. ✅ Фраза о доставке на странице оформления (§12.6) — сделано кодом P0-06.
7. ✅ Безопасная нумерация ORD/INV/CM (PostgreSQL sequences) — сделано кодом P0-07.
8. 🐛 Починить сдвиг колонок в списке компаний `/admin/companies` (баг №1).
9. 🐛 Вывести продавца по каждой позиции в карточке заказа админа + группировку комиссии по продавцам (баг №2).
10. 🐛 Добавить «Итоговую сумму НДС» и НДС по позиции в корзину (баг №3).
11. ✅ Корзина: фото товара вместо иконки-заглушки; кнопка пустой корзины «Перейти в каталог» — сделано кодом P0-11.
12. ✅ P0-12: список заказов покупателя (§17.6) дополнен НДС, кол-вом товаров, признаками счёта/УПД/спец/акта и кнопкой повтора.

**P1 — крупные функциональные дыры v1:**
8. Чат покупателя + Telegram-бот (grammY) + webhook + резервный просмотр/ответ в админке + ошибки Telegram.
9. Реальная SMTP-отправка email (worker по `email_outbox`) + шаблоны + email о регистрации/документах.
10. Восстановление пароля + смена пароля в ЛК.
11. Подгрузка данных по ИНН (адаптер DaData/ФНС) — ждёт ⛔ ключ.
12. Дашборд покупателя §17.2; дашборд админки §21.2 до полного списка.
13. Доработка PDF-счёта (реквизиты маркетплейса, КПП/ОГРН/банк, КПП/ОГРН покупателя, ставка НДС колонкой, сумма прописью, подпись, печать, логотип) — частично ждёт ⛔ материалы.

**P2 — полировка/приёмка:**
14. Полный адаптив §25, состояния экранов §26, H1/meta §34.4.
15. Блок преимуществ на главной; признаки наличия типов документов в списках заказов.
16. Комиссия от оплаченных позиций; история выплат продавца (ждёт ⛔ решение).
17. Инструкция администратора §41; бэкап; передача доступов §32.

**Ждёт заказчика (⛔):** реквизиты/подпись/печать/логотип счёта, формат №счёта, правила НДС (подтвердить 22%), модель продавца, комиссия с/без НДС, каталог-контент, пример Excel, домен/email/Telegram/доступы.
